{
  "name": "strimmer",
  "private": true,
  "type": "module",
  "version": "2026.09.25.6",
  "description": "Reverse proxy that serves the real HA frontend but trims the entity websocket to a dashboard allowlist.",
  "main": "ha_ws_trim_proxy.mjs",
  "scripts": {
    "start": "node ha_ws_trim_proxy.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "esphome-device": "^0.1.0",
    "httpxy": "^0.5.5",
    "multicast-dns": "^7.2.5",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "puppeteer-core": "^25.10.0"
  }
}
